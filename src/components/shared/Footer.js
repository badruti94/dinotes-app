import styled from "styled-components";

const Container = styled.div`
    margin: 1rem;
    padding: 0.5rem;
    min-height: 10vh;
`

const Footer = () => {
    return (
        <Container>
            <h1>Footer</h1>
        </Container>
    );
};

export default Footer